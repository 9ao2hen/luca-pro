<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>cn.mervyn.boot</groupId>
        <artifactId>luca-module-mall</artifactId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>luca-module-trade-biz</artifactId>
    <packaging>jar</packaging>

    <name>${project.artifactId}</name>
    <description>
        trade 模块，主要实现交易相关功能
        例如：订单、退款、购物车等功能。
    </description>

    <dependencies>
        <dependency>
            <groupId>cn.mervyn.boot</groupId>
            <artifactId>luca-module-trade-api</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>cn.mervyn.boot</groupId>
            <artifactId>luca-module-product-api</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>cn.mervyn.boot</groupId>
            <artifactId>luca-module-pay-api</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>cn.mervyn.boot</groupId>
            <artifactId>luca-module-promotion-api</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>cn.mervyn.boot</groupId>
            <artifactId>luca-module-member-api</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>cn.mervyn.boot</groupId>
            <artifactId>luca-module-system-api</artifactId>
            <version>${revision}</version>
        </dependency>

        <!-- 业务组件 -->
        <dependency>
            <groupId>cn.mervyn.boot</groupId>
            <artifactId>luca-spring-boot-starter-biz-tenant</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.mervyn.boot</groupId>
            <artifactId>luca-spring-boot-starter-biz-ip</artifactId>
        </dependency>

        <!-- Web 相关 -->
        <dependency>
            <groupId>cn.mervyn.boot</groupId>
            <artifactId>luca-spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>cn.mervyn.boot</groupId>
            <artifactId>luca-spring-boot-starter-security</artifactId>
        </dependency>

        <!-- DB 相关 -->
        <dependency>
            <groupId>cn.mervyn.boot</groupId>
            <artifactId>luca-spring-boot-starter-mybatis</artifactId>
        </dependency>

        <dependency>
            <groupId>cn.mervyn.boot</groupId>
            <artifactId>luca-spring-boot-starter-redis</artifactId>
        </dependency>

        <!-- Test 测试相关 -->
        <dependency>
            <groupId>cn.mervyn.boot</groupId>
            <artifactId>luca-spring-boot-starter-test</artifactId>
        </dependency>

        <!-- 工具类相关 -->
        <dependency>
            <groupId>cn.mervyn.boot</groupId>
            <artifactId>luca-spring-boot-starter-excel</artifactId>
        </dependency>
    </dependencies>

</project>
